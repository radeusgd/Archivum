package com.radeusgd.archivum.languages

/*
   For now it's one object, later we can create variants for various languages and inject them
    (like English and Polish vocabulary variants).
 */
object QueryLanguage {
   val ArrayLength = "length"
   val YDYear = "year"
   val YDFullDate = "date"
}
