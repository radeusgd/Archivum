package com.radeusgd.archivum.gui.controls
/*
import scalafx.scene.control.{ComboBox, Label, TextField}
import scalafx.scene.layout.HBox
import com.radeusgd.archivum.datamodel.types.EnumField

import scalafx.collections.ObservableBuffer
import scalafx.geometry.Pos

class EnumChoice(val name: String, val enum: EnumField) extends HBox {
   protected val choiceField = new ComboBox[String]() {
      items = ObservableBuffer(enum.enumType.values)
      value = enum.enumType.values(0)
   }
   spacing = 5
   children = Seq(
      new Label(name) {
         minWidth = 100
         alignment = Pos.CenterRight
      },
      choiceField
   )
}
*/